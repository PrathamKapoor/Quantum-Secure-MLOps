'''Identity model definitions for the IAM layer.'''
\nfrom __future__ import annotations\n\nfrom dataclasses import dataclass, field\nfrom typing import List, Optional\n\n\n@dataclass\nclass Identity:\n    id: str\n    type: str  # 'human', 'service', 'agent'\n    credentials_ref: str  # reference to a cryptographic credential\n    attributes: dict = field(default_factory=dict)\n\n\n@dataclass\nclass HumanIdentity(Identity):\n    type: str = "human"\n    name: Optional[str] = None\n    email: Optional[str] = None\n\n\n@dataclass\nclass ServiceIdentity(Identity):\n    type: str = "service"\n    service_name: Optional[str] = None\n\n\n@dataclass\nclass AgentIdentity(Identity):\n    type: str = "agent"\n    agent_name: Optional[str] = None\n